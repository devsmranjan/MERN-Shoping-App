import React from 'react';
import LogoutButton from './LogoutButton';

const UserAppbarActions = props => {
    return (
        <div>
            <LogoutButton />
        </div>
    );
};

export default UserAppbarActions;
